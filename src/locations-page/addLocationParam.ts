/** Asks the locations page to open the add-location form straight away. Lives outside
 *  the page so linking to it does not pull Leaflet into the linking bundle. */
export const ADD_LOCATION_SEARCH_PARAM = "add";
